// firebase/firestore.rules

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /staffforge_sessions/{document=**} {
      allow read, write: if true;
    }

    match /staffforge_events/{document=**} {
      allow read, write: if true;
    }
  }
}